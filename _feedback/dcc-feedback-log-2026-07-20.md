# DCC Feedback Log — 2026-07-20 (Aaron, live review)

**Captured live so nothing is lost again.** DCC is NOT launch-ready per Aaron: no beta test yet, no self UX/UI pass, information architecture questionable. Do NOT point people at DCC publicly (no DCC link in any LinkedIn post) until Aaron has done his UX pass and signed off. This log is the running list; items get fixed (mechanical) or flagged (taste/brand) then checked off.

## Status legend: [ ] open · [x] fixed+verified · [F] flagged for Aaron (taste/brand)

## Items

- [x] **Dead nav tabs — "Staying Connected" and "Getting Things Done" go nowhere when clicked.** FIXED 2026-07-21.
  - **Root cause:** the v2 primary nav carried FOUR section links (`#protect-money`, `#everyday-confidence`, `#staying-connected`, `#getting-things-done`), but the v2 index was consolidated to only TWO lesson groups — `#protect-money` and `#everyday-confidence`. The IDs `staying-connected` and `getting-things-done` **do not exist** anywhere on the page, so those two tabs were pure dead anchors (jump to a non-existent fragment = "nothing happens"). The French index (`fr/index.html`) already had only the two correct links, confirming the English nav was stale.
  - **Where it lived:** the shared page shell — so the dead links were on root `index.html` AND every `modules/*.html` page (35 pages). Build source of truth is `build/brands/trust-blue/brand.json` → `primary_nav` (injected via `{{PRIMARY_NAV_HTML}}` by `build/build.py`). `primary_nav_fr` was already correct (2 links), which is why French was fine.
  - **Fix (old → new):** removed the two lines `<a href="…index.html#staying-connected">Staying connected</a>` and `<a href="…index.html#getting-things-done">Getting things done</a>` from `index.html`, `build/brands/trust-blue/brand.json` (source of truth, keeps future rebuilds fixed), and all `modules/module-*.html`.
  - **BONUS second dead link found + removed:** 7 modules (10, 11, 12, 15, 16-travel-safety, 19-digital-legacy, 2-5) were tagged to the removed "Getting things done" group and ALSO carried a dead **breadcrumb** link to `#getting-things-done`. Those breadcrumbs now read `All lessons › [page]`.
  - **Full nav sweep — no other dead links:** all remaining primary-nav + footer targets on `index.html` resolve (`for-families.html`, `support-directory.html`, `youth/index.html`, `about.html`, `faq.html`, `glossary.html`, `privacy.html`, `terms.html`, `disclaimer.html`, `fr/index.html`, `favicon.svg`, `scam-defence-helper.html`, all `modules/*` card links). Both surviving anchors exist on the page.
  - **Verified (Playwright, local serve):** `index.html` nav now `[All lessons, Protect your money, Everyday confidence, For families, Get help]`, deadAnchors `[]`. Module page confirmed identical clean nav. Screenshot: `quality/ai-lit-full.png`.
  - **Left alone on purpose:** the same two dead links still exist in `dist/trust-blue/*.html` (33 files) — a generated build artifact, NOT the live path; it regenerates correctly from the fixed `brand.json` on next `build.py`. Flagged so it isn't mistaken for a miss.
  - **IA note:** those 7 "Getting things done" modules are now effectively orphaned on the homepage (reachable by URL, not surfaced in any index group). Whether to reintroduce the group(s) + tabs or fold them into the two existing groups is a content/IA decision — parked for the IA audit item, not fixed mechanically.

- [F] **Stray blue line, top-left corner of the AI-literacy page — COULD NOT REPRODUCE; needs Aaron to confirm which line.** Investigated 2026-07-21.
  - The live v2 "What Is AI, Really?" lesson is `modules/module-ai-literacy.html` (the root `module-ai-literacy.html` is only a redirect stub to the `classic/` archive; the v2 index card links to the `modules/` page). Inspected in-browser (light + dark theme), plus the live GH Pages copy and the classic version.
  - **No stray blue line/border found in the top-left corner.** Computed-style + full-DOM scan for any thin blue hairline in the upper page area returned nothing. `html`/`body`/`header`/`container` have no `border-top`/`border-left` and no `::before`. The skip-link (blue) sits at `top:-60px` (fully hidden). Screenshots: `quality/tl-corner.png`, `quality/tl-tiny.png`, `quality/tl-dark.png`, `quality/live-tl.png`.
  - **The only blue-ish lines are intentional + site-wide:** (a) header bottom divider `1.6px solid #d5dde6` (light blue-grey) + faint shadow; (b) primary-nav bottom divider `0.8px solid #d5dde6`; (c) consent-bar `3px` blue top border (at the BOTTOM of the viewport). None is a "stray" mistake and none is unique to this page.
  - **Left for Aaron:** removing a site-wide divider is a design change, not a surgical fix, so nothing was changed. Please confirm which line (marked screenshot / exact viewport + theme helps). If it's the header divider you want softened, that's a one-line token change in `css/core.css`, applied site-wide — say the word.

- [F] **Logo / brand mark not locked.** DCC ("Digital Confidence Centre") has no logo yet. Is the current "fabric"/motif on the site meant to BE the logo? Aaron wants this confirmed and locked into the brand. → Confirm what mark is currently used, present it, Aaron decides + we lock it in DESIGN-SYSTEM / brand.
  - **FINDINGS 2026-07-21 — a purpose-built mark EXISTS, but is used ONLY as the favicon, never as a visible on-page logo:**
  - **The mark:** `C:\twobirds\digital-confidence\favicon.svg` — its own SVG comment names it the **"DCC Adult v2 mark"**: a Trust Blue (`#1D4E89`) rounded-square **shield (safety) with a calm checkmark (confidence / "you did it")**, "deliberately distinct from the retired Two Birds logo and from the playful DCC Kids treatment." A real mark WAS intentionally designed. Rendered large for review: `C:\twobirds\digital-confidence\quality\dcc-mark.png`.
  - **How it's used today:** ONLY as the favicon (browser-tab / bookmark icon) — referenced by 71 non-classic pages. It is **not rendered visibly anywhere on-page.**
  - **The visible "logo" is actually text:** the header brand is a **text wordmark** — `<a class="brand">Digital Confidence Centre</a>` — styled Trust Blue via CSS. No image logo in the header.
  - **The "fabric"/hero motif is NOT a logo:** the `index.html` hero is a separate inline hand-drawn line-art SVG (senior with a tablet, mug, plant; `currentColor` strokes) — decorative, page-specific. No repeating "fabric" pattern asset exists in the CSS.
  - **For your call:** the shield-check mark is the natural candidate to lock in as THE DCC logo (shield = safety, check = confidence, Trust Blue = brand). Locking it in = (a) place it visibly in the header beside/above the wordmark, (b) document it. **NOT locked until you say so — taste call.**
  - **Where to document when locked:** brand colour token is already in `C:\twobirds\digital-confidence\css\tokens.css` (`--color-primary: #1D4E89`, "Trust Blue"). `C:\twobirds\digital-confidence\DESIGN-SYSTEM.md` currently has **no logo/brand-mark section** — locking in later = add a "Brand mark / logo" section there pointing at `favicon.svg` + `#1D4E89`, and add `<img src="favicon.svg">` to the header in `build/templates/module-v2.html.tpl` + `index.html`.

- [F] **"For Families" tab — audience-shift / IA + psychology problem (Aaron, strong flag).** The whole site speaks TO the senior/primary user. "For Families" sits IN-LINE with the other nav items, so a senior clicking it hits content aimed at a *different* audience (a loved one supporting them) with no signpost — confusing ("Am I getting this for my family? No, it's for YOUR family"). Two fixes wanted:
    1. **Nav treatment:** do NOT keep "For Families" in-line with the other menu options. Differentiate/delineate it — e.g. a hanging tab, or set off to the right / below / visually distinct — so it reads as a separate doorway for a different person, without beating them over the head.
    2. **On the page itself:** make the audience shift immediately clear so a senior who stumbles in instantly knows it's not speaking to them. Open with something like *"Do you have a loved one who could use some support with technology? This section is for you."* Possibly make the page look visually different from the rest of the site (no image required). Frame: "Are you a loved one supporting someone who needs help? This section is for you."
    IA + psychology treatment — route through the IA GATE (`/information-architecture`) + DESIGN GATE since it restructures nav; propose before building. Aaron challenges the current treatment.

- [ ] **UX/UI not yet self-reviewed by Aaron / no beta tester.** Full UX/UI audit to surface issues systematically (recover the lost feedback batch).

- [F] **"For Families" page — section-footer CTA/info treatment (Aaron, design flag + "give it a crack").** The 3 sections each have an oval cream-coloured button at the bottom, **left-justified**, and they're **cramming information into a call-to-action shape**. Problems + wants:
    1. **Separate INFO from ACTION.** Time-to-complete and "certificate at the end" are *information*, not CTAs — don't put them in a button/oval shape. Present info subtly/differently (e.g. a light-grey "…" dotted-line treatment / small caption), reserve the button shape for the ACTUAL call to action (Get Started / Learn More).
    2. **One message per element — don't cram.** Time estimate = its own message, CONSISTENT across all 3 sections. The "certificate at 85%" note is fine but as its OWN message, not mashed together. Currently inconsistent: two sections mention time, one mentions the certificate.
    3. **Rename "certificate."** "Certificate" sounds too official/overstated → use **"completion badge"** or "completion certificate" (Aaron leans badge; his call).
    4. **Alignment:** the CTA button should be **centered or bottom-right, NOT bottom-left.**
    5. **Give it a crack:** research who does course-card footers well (best practice) + use the available UX design tools (Anthropic frontend-design skill, design-languages reference) and take another shot. Propose for Aaron's review (DCC design gate).

## Notes
- Prior DCC feedback batch (nav/delineation, read-aloud placement, logos, contrast, iPhone-default, "back to DCC" broken, video style) was given earlier this session — cross-reference `digital-confidence/_feedback/` for existing items so we don't duplicate.
- Fixes to the LIVE site must pass Playwright live-verification (LIVE-OUTCOME VERIFICATION rule) + bump service-worker CACHE_NAME so returning visitors get the fix. **Update 2026-07-21:** the v2 root `sw.js` is a **no-cache kill-switch** — on activate it deletes every cache and unregisters itself, so root pages are ALWAYS served fresh from the network. There is no CACHE_NAME to bump and returning visitors are not stuck on a stale cache, so no SW change was needed for these fixes.
- **Pre-existing uncommitted work left untouched (NOT part of this fix — flagged for Aaron):** the repo already had in-progress uncommitted changes before this pass — a French translation build for Module 2 (`modules/module-2-fr.html` untracked, `build/content/modules-v2/module-2.json` +515 lines, hreflang/lang-link edits in `modules/module-2.html`), plus edits to three `classic/` files (`facilitator-guide.html`, `for-libraries.html`, `institutional.html`). These were left as-is and NOT committed with the nav fix. Module 2's nav fix was staged cleanly on its own (without sweeping in the Module-2 French WIP). Aaron: decide when to finish/commit the Module 2 French translation + the classic edits.

- [F] **Action-button verbiage inconsistency — unify to one IA action vocabulary (Aaron, 2026-07-21, URL to follow).** Aaron appreciates the recent updates (executed mostly right). On the DCC *adult* confidence centre, the action buttons across the bottom for the primary "actions" — **[view] modules / view the course / see kids' lessons** — plus the per-item CTAs (**"Open lesson" / "Start practicing" / "Open lesson"**) use near-synonym verbiage for what feels like the same action. His read: *"Open lesson, start practicing, open lesson — to me it feels the same... a needless strain on the brain."* Wants:
    1. **Common language across all the actions.** Pick a single consistent action vocabulary (per a standard IA / UX approach or component library) that covers modules, course, and kids'-lessons actions — one verb pattern, not three near-synonyms. This is his stated preference.
    2. **Right-justification:** he DID want the actions right-justified (the treatment I applied) — likes it. Open question (voice was garbled): he "only wanted [it on one set]" and noted the overlap ("start practice overlapping... we could do those as well") — i.e. possibly apply the same right-justify treatment consistently to the "Start practicing"/practice-overlap buttons too. **Confirm scope once URL is in hand.**
    - **Treatment:** IA + microcopy consistency — route through IA GATE (`/information-architecture`) for the action-label taxonomy + DESIGN GATE; propose the unified label set for Aaron's review before editing live. Blocked pending Aaron's URL + right-justify-scope confirmation. Research who does course-action labelling well (consistent verb families) per the frontend-design skill.
